{
  "name": "chatbot",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "python": "cd SpacyModel && venv\\Scripts\\activate && python app.py",
    "backend": "cd backend && npm install && npm run server",
    "frontend": " cd frontend && npm install && npm run dev",
    "bot": "start cmd /k \"npm run python\" && start cmd /k \"npm run backend\" && start cmd /k \"npm run frontend\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
